import React, { Component } from "react";
import { connect } from "react-redux";

class AccountsContainer extends Component {
  render() {
    return <>Accounts</>;
  }
}

export default connect()(AccountsContainer);
